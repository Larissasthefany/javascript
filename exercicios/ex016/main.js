var altura = Number(prompt("Qual a altura da parede?"))
var largura = Number(prompt("Qual a largura da parede?"))

var result = (altura * largura) /2

alert(`A  altura da parede é de ${altura}M e ${largura} de largura, então sera necessario ${result} litros de tinta` )